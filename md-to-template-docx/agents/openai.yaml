interface:
  display_name: "Markdown to Template DOCX"
  short_description: "Build DOCX with captions and LaTeX placeholders"
  default_prompt: "Use $md-to-template-docx to convert Markdown into a Word template or bundled default template, preserving LaTeX for MathType and converting figure/table captions to Word Caption fields."
